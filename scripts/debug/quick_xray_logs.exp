#!/usr/bin/expect -f
set timeout 10
set ip [lindex $argv 0]
set password [lindex $argv 1]

spawn ssh -o StrictHostKeyChecking=no root@$ip
expect "password:"
send "$password\r"

expect "#"
send "docker logs xray-vpn --tail 100 2>&1\r"
expect "#"

send "exit\r"
expect eof
