#!/usr/bin/expect -f
set timeout 20
set password "Tanquzsacjev5hywvo"
set host "root@37.60.251.4"

# Try to read the file directly from the likely path
spawn ssh -o StrictHostKeyChecking=no $host "cat /root/ess-config-values/ess-values.yaml"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
